import React from 'react'
import { Button } from '@/components/ui/button'

type Props = {}

const page = (props: Props) => {
  return (
    <div>HI LOL
      <Button> Click LOL </Button>
    </div>
  )
}

export default page